
/**
 * 配置
 */

var config = {
    
    // static
    'static_dir': '/static/',

    'output': {
        'base': {
            'source': 'static',
            'target': 'html',
        },
        'lib': 'lib',
        'view': 'view'
    }
}

module.exports = config;
