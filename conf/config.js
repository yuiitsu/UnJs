
/**
 * 配置
 */

var config = {
    'version': '1.0.0',
    // static
    'static_dir': '/static/',

    'output': {
        'base': {
            'source': 'static',
            'target': 'html'
        },
        'lib': 'lib',
        'view': 'view'
    }
};

module.exports = config;
